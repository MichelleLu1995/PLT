def int main() {
	String line;
	String temp;

	int num;

	num = 1234;
	line = "1234";
	print(atoi(line));
	prints("\n");
	print(len(line));

	temp @= new[4];
	itos(temp, "%d", num);
	prints(temp);

	prints("\n");

	return 0;
}
